{
  "name": "azuryapi",
  "version": "1.0.1",
  "description": "The official way of using Azury API 4 Node.JS",
  "main": "src/index.js",
  "homepage": "https://api.azury.cc/",
  "types": "./src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "test": "ts-node ./test/testAPI",
    "test:java": "node ./test/testAPIJS",
    "lint": "prettier --write --cache . && eslint --cache --fix .",
    "typecheck": "tsc -p tsconfig.eslint.json"
  },
  "author": "AzuryIndustries",
  "files": [
    "src/**/*.js",
    "src/**/*.d.ts",
    "src/**/*.js.map"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "azuryapi": "^1.0.1",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^20.4.1",
    "ts-node": "^10.9.2",
    "tsx": "^3.12.7"
  }
}
